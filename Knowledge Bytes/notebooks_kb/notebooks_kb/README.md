# GraphRAG `notebooks` Knowledge Bytes

The uploaded `notebooks` directory contains one relevant notebook:

```text
notebooks/
└── evaluation_report.ipynb
```

The notebook is documented below as:

```text
notebooks_kb/
├── README.md
└── evaluation/
    └── evaluation_report.md
```

The notebook is an evaluation/reporting layer. It reads artifacts produced by `src.evaluation.compare`; it does not itself run the GraphRAG retrieval pipeline.
