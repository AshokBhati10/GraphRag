# GraphRAG `lib` Knowledge Bytes

This package contains focused Knowledge Bytes for the relevant modules/assets found under `lib/`.

```text
lib_kb/
├── README.md
├── bindings/
│   └── utils.md
├── vis-9.1.2/
│   └── vis-network.md
└── tom-select/
    └── tom-select.md
```

The vendored/minified third-party libraries are documented at the responsibility/API level rather than reproduced line-by-line. The custom `bindings/utils.js` receives the most detailed treatment because it contains project-specific behavior.
